-- TASK 1
CREATE DATABASE ONLINE_STORE;

-- TASK 2
CREATE TABLE USERS (
	ID INT NOT NULL,
	NAME NVARCHAR(50) NOT NULL,
	PRIMARY KEY (ID)
);

-- TASK 3 + TASK 4
ALTER TABLE USERS
ADD EMAIL NVARCHAR(50) NOT NULL UNIQUE

-- TASK 5 + TASK 6
CREATE TABLE PRODECTS(
	ID INT PRIMARY KEY,
	NAME NVARCHAR(50) NOT NULL ,
	PRICE DECIMAL(10,2) NOT NULL,
);

-- TASK 7
CREATE TABLE ORDERS(
	ID INT PRIMARY KEY  NOT NULL,
	USER_ID INT NOT NULL,
	FOREIGN KEY (USER_ID) REFERENCES USERS(ID)
);

-- TASK 8 + TASK 9 
ALTER TABLE ORDERS
ADD ORDERS_DATE DATETIME NOT NULL DEFAULT GETDATE();

-- TASK 10 

-- TASK 11 + 12 + 13
CREATE TABLE CATEGORIES(
	ID INT PRIMARY KEY ,
	NAME NVARCHAR(59) NOT NULL
);
ALTER TABLE PRODECTS
ADD CATEGORY_ID INT NOT NULL,
FOREIGN KEY (CATEGORY_ID) REFERENCES CATEGORIES(ID)

CREATE TABLE employees (
	ID INT PRIMARY KEY ,
	NAME_EMPLOYEE NVARCHAR(50) NOT NULL,
	NAME_MANGER NVARCHAR(5) NOT NULL,
	FOREIGN KEY (NAME_MANGER) REFERENCES employees (ID)
);